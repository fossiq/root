@local tokens {
<% for (const token of it.localTokens) { %>
  <%= token.name %><% if (token.dialect) { %>[@dialect=<%= token.dialect %>]<% } %> { <%- it.serializePattern(token.pattern) %> }
<% } %>
}
