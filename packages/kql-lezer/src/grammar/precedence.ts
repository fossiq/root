import { type PrecedenceLevel } from "@fossiq/lezer-grammar-generator";

/**
 * Precedence configuration for Lezer grammar generation.
 * Currently no operator precedence is enforced.
 */
export const precedence: PrecedenceLevel[] = [];
