{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "dist/**",
        "**/*.wasm",
        "**/*.node",
        "grammar.js",
        "bindings/**",
        "prebuilds/**",
        "packages/kql-lezer/src/kql.grammar",
        "packages/kql-lezer/src/parser.ts",
        "packages/kql-lezer/src/parser.terms.ts",
        "!**/node_modules/**"
      ],
      "inputs": ["src/**", "package.json", "tsconfig.json", "turbo.json"]
    },
    "test": {
      "dependsOn": [],
      "outputs": [],
      "inputs": ["src/**", "tests/**", "turbo.json"]
    },
    "test:coverage": {
      "dependsOn": [],
      "outputs": ["coverage/**"],
      "inputs": ["src/**", "tests/**", "turbo.json"]
    },
    "lint": {
      "outputs": [],
      "inputs": ["src/**", "turbo.json"]
    },
    "ci:publish": {
      "outputs": [],
      "cache": false
    }
  }
}
